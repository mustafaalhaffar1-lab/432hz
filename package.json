{
  "name": "432hz-store",
  "version": "1.0.0",
  "private": true,
  "description": "432Hz — luxury wellness storefront + admin (zero-dependency Node server)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
